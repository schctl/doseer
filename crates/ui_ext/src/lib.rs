//! UI related extensions such as extra widgets, helper traits, etc. Basically anything that doesn't
//! have to do with the actual UI of `m7` but adds/builds functionality on top of [`iced`](iced_native).

pub mod components;
pub mod widgets;
