//! UI related extensions such as extra widgets, helper traits, etc. Basically anything that doesn't
//! have to do with the actual UI of `doseer` but adds/builds functionality on top of [`iced`](iced_core).

pub mod components;
pub mod widgets;
